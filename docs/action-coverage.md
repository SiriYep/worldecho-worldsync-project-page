# Action coverage: source and implementation status

13 September 2026: the user clarified that the requested dynamic scatter/distribution concerns action-query coverage in paper Figure 5c. The earlier seven-model mean charts did not satisfy this request and have been removed. The page currently shows the original paper panel; **interactive category highlighting and action distributions remain incomplete**.

## Verified source

- Source: `Paper-AF/assets/figure5.pdf`, also published in [arXiv v1 Figure 5](https://arxiv.org/html/2608.24885v1#S4.F5).
- Original PDF copied unchanged to `assets/figures/paper-figure-5.pdf`.
- PDF SHA-256: `645f8b8868b1fdf2094d09e57c636d0c281f28378b31342be5eb8431ef0231da`.
- `paper-figure-5c.png` renders the original panel at 288 dpi: PDF rectangle x=320–507.6, y=0–158.4 pt. Poppler command: `pdftoppm -f 1 -singlefile -cropbox -r 288 -x 1280 -y 0 -W 751 -H 634 -png figure5.pdf figure5c`. No points, colors, labels, or contours were redrawn.
- PC1 explains 22.9% and PC2 18.0% of variance. Dashed contours are expert/off-expert 95% HDR, as labeled in the paper.
- [WorldSimProbe](https://evophys.com/WorldSimProbe/#motivation) provides a reference for presenting the action-distribution gap. Its figure is a static JPEG; its data must not be substituted for WorldEcho data.

| Category | Paper marker | Meaning |
| --- | --- | --- |
| Expert | Blue circle | Demonstrated action associated with the current observation |
| Cross-state replay | Green circle | Expert action sequence replayed from another state |
| Local perturbation | Purple triangle | Bounded changes around demonstrated actions |
| Policy rollout | Pink square | Actions generated by a learned policy |
| Feasible-space sampling | Gold diamond | Feasible actions sampled from the broader action space |

Definitions follow paper section 3.3, Action Query Construction.

## Intended interaction and required data

Clicking a category highlights its entire point cloud and fades the other categories. Clicking the same category again, or an All control, restores the full plot. Category names and marker shapes remain available for keyboard access and color-independent identification. Matching distribution layers should follow the same category selection once their plotted quantity and source are verified.

Required from the figure owner: original plotting script and input files, or an exported point table with `pc1`, `pc2`, and the five-category label (preferably `sample_id`/`task` too). Preserve the original PCA preprocessing, fitted projection, variance ratios, and the method/data for the distribution/HDR layers. Do not invent task counts, per-point provenance, PCA coordinates, density estimates, or time-series animation.

## Retrieval audit

The current PDF and all matching local copies contain a single 141×104 raster image for the point cloud. Overlapping points are already merged; only the two expert and three off-expert HDR contour paths remain vector. Thus the figure is suitable for faithful static display, but cannot yield exact per-point coordinates and categories.

Local paper history was checked: `be49641` named this same asset `figure4.pdf`; `24bdc4a` renamed it `figure5.pdf`. Both are byte-identical to the current source. No PCA/HDR plotting script or point data was found in the inspected local paper branches/history or targeted workspace search.

Read-only SSH attempts to the project's known endpoints on 13 September timed out: `unified_mot_gyc` (10.40.1.76:333), `gyc_no_gpu` (:2978), and `gyc_8gpu_1` (:8544), each with an 8-second connection timeout and port forwarding disabled. Remote files were not inspected. This is a connectivity limitation, not evidence that the data is absent from the server. No remote files, runs, or services were modified.
